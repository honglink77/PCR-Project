# Review package: 9871e337a59d07e181acdec46c316f80da2e94e9..HEAD

## Commits
4cd9813 feat: Product Ops 卡片跳转 PCR Workbench

## Files changed
iProduct_Project/src/components/workspace/WorkspaceView.tsx | 4 ++++
 iProduct_Project/src/data/agents.ts                         | 1 +
 iProduct_Project/src/types/agent.ts                         | 1 +
 3 files changed, 6 insertions(+)

## Diff
diff --git a/iProduct_Project/src/components/workspace/WorkspaceView.tsx b/iProduct_Project/src/components/workspace/WorkspaceView.tsx
index a2d32f1..113cfd7 100644
--- a/iProduct_Project/src/components/workspace/WorkspaceView.tsx
+++ b/iProduct_Project/src/components/workspace/WorkspaceView.tsx
@@ -16,20 +16,24 @@ const iconMap: Record<string, React.ReactNode> = {
   Rocket: <Rocket className="w-5 h-5" />,
   Settings: <Settings className="w-5 h-5" />,
   BookOpen: <BookOpen className="w-5 h-5" />,
 };
 
 export function WorkspaceView() {
   const appDispatch = useAppDispatch();
   const { openDraft } = useChat();
 
   function handleWorkbenchClick(agent: Agent) {
+    if (agent.externalUrl) {
+      window.location.assign(agent.externalUrl);
+      return;
+    }
     openDraft(agent.id);
     appDispatch({ type: 'NAVIGATE', view: 'chat' });
   }
 
   const hours = new Date().getHours();
   const greeting = hours < 12 ? 'Good morning' : hours < 17 ? 'Good afternoon' : 'Good evening';
 
   return (
     <div className="max-w-6xl mx-auto px-8 py-8">
       {/* Greeting */}
diff --git a/iProduct_Project/src/data/agents.ts b/iProduct_Project/src/data/agents.ts
index 1128329..033978e 100644
--- a/iProduct_Project/src/data/agents.ts
+++ b/iProduct_Project/src/data/agents.ts
@@ -38,20 +38,21 @@ export const agents: Agent[] = [
     capabilities: ['Release Planning', 'Version Control', 'Rollout Strategy'],
   },
   {
     id: 'product-ops',
     name: 'Product Ops',
     description: 'Product Ops Workbench',
     icon: 'Settings',
     color: 'bg-teal-600',
     status: 'active',
     capabilities: ['Process Optimization', 'Tool Management', 'Data Governance'],
+    externalUrl: '/workbench/pcr-workbench-proto.html',
   },
   {
     id: 'knowledge-management',
     name: 'Knowledge Management',
     description: 'Knowledge Management Workbench / Module',
     icon: 'BookOpen',
     color: 'bg-indigo-500',
     status: 'active',
     capabilities: ['Knowledge Base', 'Document Hub', 'Best Practices'],
   },
diff --git a/iProduct_Project/src/types/agent.ts b/iProduct_Project/src/types/agent.ts
index ddc9809..135af69 100644
--- a/iProduct_Project/src/types/agent.ts
+++ b/iProduct_Project/src/types/agent.ts
@@ -1,20 +1,21 @@
 export type AgentStatus = 'active' | 'idle' | 'error' | 'maintenance';
 
 export interface Agent {
   id: string;
   name: string;
   description: string;
   icon: string;
   color: string;
   status: AgentStatus;
   capabilities: string[];
+  externalUrl?: string; // 若存在，Workbench 卡片外跳而非 openDraft
 }
 
 export interface AgentRun {
   id: string;
   agentId: string;
   agentName: string;
   status: 'running' | 'completed' | 'failed' | 'cancelled';
   startedAt: number;
   completedAt: number | null;
   durationMs: number | null;
